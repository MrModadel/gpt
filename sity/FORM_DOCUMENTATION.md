# Документация системы обработки форм

## Обзор системы

Система состоит из трёх компонентов:

1. **HTML формы** на страницах сайта
2. **JavaScript (main.js)** - обработчик событий и навигация по форме
3. **PHP (telegram-send.php)** - обработчик данных и отправка в Telegram

---

## Архитектура

### 1. Frontend (JavaScript)

**Файл:** `c:\frilans\termo\sity\termoservis-wp\js\main.js`

#### Функции:

**a) Обработка простых форм (.form-tel)**
- Перехватывает submit события
- Собирает данные через FormData (включая файлы)
- Отправляет POST запрос на `telegram-send.php`
- Добавляет параметр `formType` для идентификации
- Показывает алерт при успехе

**b) Многошаговая форма (#technicalRequestForm)**
- Управление переходами между 5 шагами
- Валидация обязательных полей перед переходом
- Анимация переходов и скролл к форме
- Обработка загрузки файлов (отображение названий)
- Отправка полной формы на финальном шаге
- Генерация номера заявки (TZ-[timestamp])
- Показ экрана успеха с номером заявки

#### События и кнопки:
- `nextToStep{2-5}` - переход вперёд с валидацией
- `backToStep{1-4}` - переход назад без валидации
- `submit` на форме - финальная отправка

---

### 2. Backend (PHP)

**Файл:** `c:\frilans\termo\sity\termoservis-wp\telegram-send.php`

#### Особенности:

**Универсальность:**
- Обрабатывает ЛЮБЫЕ POST параметры
- Не требует предварительного знания названий полей
- Автоматически форматирует вывод

**Обработка данных:**

1. **Основная информация** (имя, телефон, почта и т.д.)
   - Форматируется с эмодзи
   - Выводится в простом формате

2. **Множественные выборы** (checkboxes)
   - Объединяются в единую строку через запятую
   - Выводятся отдельной секцией

3. **Текстовые поля** (textarea и длинные поля)
   - Выводятся в отдельной секции
   - HTML экранируется для безопасности

4. **Файлы**
   - Каждый файл отправляется отдельным документом в Telegram
   - Поддерживаются файлы до 50 МБ
   - Множественная загрузка (multiple)

**Форматирование сообщения:**

```
📩 [Тип формы]
━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🔹 Основная информация:
• Имя: <значение>
• Телефон: <значение>
...

✓ Выбранные опции:
• Опция 1: <значение>
• Опция 2: <значение>
...

📋 Дополнительная информация:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Комментарии:
<многострочный текст>

━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🕐 Время отправки: дата время
🌐 IP: адрес
```

---

## Использование на странице

### Для простых форм:

```html
<form class="form-tel">
  <input type="text" name="name" placeholder="Имя" required>
  <input type="tel" name="phone" placeholder="Телефон" required>
  <input type="email" name="email" placeholder="Email" required>
  <input type="file" name="document">
  <button type="submit">Отправить</button>
</form>
```

**Важно:** Название атрибута `name` автоматически преобразуется в красивый заголовок поля

### Для многошаговой формы:

Форма на странице `Nestandartnie-chileri.html` содержит 5 шагов:

1. **Контактные данные** - requestName, requestPhone, requestEmail, requestCompany, requestPosition
2. **Технические параметры** - requestTemp, requestFlow, requestPower, requestMedia
3. **Особые требования** - specialReq (checkboxes), requestNotes
4. **Документы** - tzFile, drawingsFile, specFile (файлы)
5. **Сроки и бюджет** - requestTimeline, requestBudget, requestFunding, finalNotes

---

## Кастомизация

### Добавление новой формы:

1. Добавьте класс `form-tel` на форму
2. Используйте атрибут `name` для каждого поля
3. Опционально добавьте `<input type="hidden" name="formType" value="Описание формы">`

```html
<form class="form-tel">
  <!-- ваши поля -->
</form>
```

### Добавление новых полей в многошаговую форму:

1. Добавьте input/select/textarea с уникальным `id` и `name`
2. Убедитесь что `name` атрибут присутствует
3. Для файлов используйте `type="file"` с нужным расширением в `accept`
4. Для текста > 100 символов автоматически выводится в отдельной секции

### Смена Telegram чата:

Отредактируйте в `telegram-send.php`:
```php
$bot_token = 'YOUR_TOKEN_HERE';
$chat_id   = 'YOUR_CHAT_ID';
```

---

## Валидация

### Frontend (JavaScript):
- Проверяются поля с атрибутом `required`
- Checkboxes проверяются как группа по `name`
- Пользователь видит красную границу невалидных полей
- Алерт с просьбой заполнить обязательные поля

### Backend (PHP):
- Пустые значения пропускаются
- HTML экранируется для textarea полей
- Файлы проверяются на размер (до 50 МБ)

---

## Примеры POST данных

### Простая форма:
```
name=Иван Петров
phone=+79123456789
email=ivan@example.com
formType=Форма обратной связи
```

### Многошаговая форма:
```
requestName=Иван Петров
requestPhone=+79123456789
requestEmail=ivan@example.com
requestCompany=ООО Компания
requestPosition=Директор
requestTemp=+25°C / +5°C
requestFlow=10
requestMedia=water
specialReq[]=explosion
specialReq[]=corrosion
requestNotes=Дополнительная информация...
finalNotes=Комментарии...
formType=Техническое задание на расчет
```

---

## Тестирование

### Шаг 1: Убедитесь что форма подключена
```html
<script src="/wp-content/themes/termo/js/main.js"></script>
```

### Шаг 2: Проверьте консоль браузера (F12 → Console)
- Наличие ошибок JavaScript
- Успешность fetch запроса

### Шаг 3: Проверьте Telegram
- Сообщение должно прийти в указанный чат
- Файлы должны загрузиться отдельно

### Шаг 4: Проверьте валидацию
- Попробуйте отправить форму без обязательных полей
- Должно показаться окно ошибки и красные границы

---

## Ошибки и решения

### "Ошибка отправки, проверьте PHP"
- Проверьте токен бота в `telegram-send.php`
- Проверьте chat_id
- Убедитесь что бот добавлен в чат
- Проверьте интернет соединение

### Файлы не загружаются
- Проверьте размер файла (max 50 МБ)
- Убедитесь что `name` атрибут присутствует на input type="file"
- Проверьте расширение файла в `accept` атрибуте

### Форма не переходит на следующий шаг
- Проверьте что все обязательные поля отмечены `required`
- Смотрите красные границы - это невалидные поля
- Проверьте консоль на ошибки JavaScript

### Данные приходят неправильно
- Убедитесь что у каждого input есть атрибут `name`
- Название `name` должно быть на латинице без пробелов
- Для checkboxes группа должна иметь одинаковый `name`

---

## Безопасность

1. **CSRF защита:** Используйте на сервере CSRF токены (рекомендуется)
2. **Валидация:** Все данные проверяются на сервере перед отправкой
3. **HTML экранирование:** Используется для textarea полей
4. **Размер файлов:** Ограничен 50 МБ
5. **Rate limiting:** Рекомендуется добавить на сервере

---

## Поддерживаемые типы данных

| Тип | Пример | Обработка |
|-----|--------|-----------|
| Text input | имя, телефон | В основную информацию |
| Number input | расход, мощность | В основную информацию |
| Email | почта | В основную информацию |
| Tel | телефон | В основную информацию |
| Select | выбор | В основную информацию |
| Checkbox (группа) | требования | В "Выбранные опции" |
| Textarea (>100 сим) | комментарии | В "Дополнительная информация" |
| File single | ТЗ | Отдельный документ |
| File multiple | чертежи | Несколько документов |

---

**Версия документации:** 1.0  
**Дата обновления:** 2026-02-05  
**Автор:** GitHub Copilot
